// Este archivo no contiene pruebas actualmente para el componente LoginComponent.
// Si necesitas añadir pruebas en el futuro, puedes configurarlas aquí.
describe('LoginComponent', () => {
  it('Sin pruebas definidas', () => {
    expect(true).toBeTrue();
  });
});
